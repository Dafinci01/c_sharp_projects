# Creating a New C# Console App with .NET CLI

To create a new C# console application from the terminal, use:

```bash
dotnet new console -n MyAppName
# Creating a New C# Console App with .NET CLI

To create a new C# console application from the terminal, use:

dotnet new console -n MyAppName

